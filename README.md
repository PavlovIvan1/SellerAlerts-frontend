# Seller Alert Frontend

## Описание системы

Дашборд для мониторинга показателей продавцов с **токен-based аутентификацией**. Система использует токены доступа для добавления новых продавцов.

## Быстрый старт

### Установка зависимостей
```bash
npm install
```

### Запуск в режиме разработки
```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:5173`

### Сборка для продакшена
```bash
npm run build
```

## Система аутентификации

### Примеры токенов для тестирования

| Токен | Компания | Статус |
|-------|----------|--------|
| `tok_ivanov_2024_abc123` | ИП Иванов | Активно |
| `tok_petrova_2024_def456` | ООО Петрова | Активно |
| `tok_sidorov_2024_ghi789` | ИП Сидоров | Неактивно |
| `tok_company_2024_jkl012` | ООО ТехСтарт | Активно |

## Управление

### Навигация между продавцами
- **Свайпы**: проведите пальцем влево/вправо
- **Клавиши**: используйте стрелки ← →
- **Табы**: кликните на вкладку продавца

## Технологии

- **React 19** - основная библиотека
- **TypeScript** - типизация
- **Vite** - сборщик
- **CSS Modules** - стилизация
- **CSS Variables** - дизайн-система

## Структура проекта

```
src/
├── components/
│   ├── ui/           # UI компоненты
│   │   ├── Header/   # Заголовок приложения
│   │   ├── Tabs/     # Вкладки продавцов
│   │   ├── TokenModal/ # Модальное окно для ввода токенов
│   │   └── SellerCard/ # Карточка метрики
│   └── sections/     # Секции приложения
│       ├── People/   # Список продавцов
│       ├── SellerCards/ # Карточки метрик
│       └── CompanyInfo/ # Информация о компании
├── data/             # JSON файлы с данными
│   ├── tokens.json   # 🔐 База токенов доступа
│   ├── people.json   # 👥 Список подключенных продавцов
│   ├── cardsByPerson.json # 📊 Метрики по продавцам
│   ├── account.json  # 🏠 Данные пользователя
│   ├── names.json    # 📋 Короткие имена продавцов
│   ├── categoriesByPerson.json # 📂 Данные по категориям товаров
│   └── productsByPerson.json # 🛒 Данные по товарам
├── styles/           # Глобальные стили
└── App.tsx          # Главный компонент
```

## 📁 Полное описание JSON файлов

### 📋 names.json - Короткие имена
Простое соответствие ID продавца к короткому имени:
```json
{
  "ivanov": "Иван",
  "petrova": "Мария",
  "sidorov": "Алексей",
  "company1": "Компания ТехСтарт"
}
```

### 🏠 account.json - Данные пользователя
Личная информация о пользователе системы:
```json
{
  "user": {
    "firstName": "Дмитрий",
    "fullName": "Васильев Дмитрий Александрович",
    "dateOfBirth": "02.01.1993"
  }
}
```

**Поля пользователя:**
- `firstName` - имя для обращения
- `fullName` - полное ФИО
- `dateOfBirth` - дата рождения в формате DD.MM.YYYY

### 🛒 productsByPerson.json - Данные по товарам
Метрики по конкретным товарам каждого продавца:
```json
{
  "ivanov": [
    {
      "id": "product_1",
      "name": "Рубашка мужская белая",
      "orders": 24,
      "ordersAmount": "72,000 ₽",
      "drr": "15.3%",
      "ctr": "2.8%",
      "cpc": "45 ₽"
    }
  ]
}
```

**Поля товара:**
- `id` - уникальный идентификатор
- `name` - название товара
- `orders` - количество заказов
- `ordersAmount` - общая сумма заказов
- `drr` - ДРР (Доля Рекламных Расходов)
- `ctr` - CTR (Click Through Rate)
- `cpc` - CPC (Стоимость за клик)

### 📂 categoriesByPerson.json - Данные по категориям
Метрики по категориям товаров каждого продавца:
```json
{
  "ivanov": [
    {
      "id": "category_1",
      "name": "Мужская одежда",
      "orders": 45,
      "ordersAmount": "270,000 ₽",
      "drr": "14.2%",
      "ctr": "3.1%",
      "cpc": "42 ₽"
    }
  ]
}
```

**Поля категории:**
- `id` - уникальный идентификатор
- `name` - название категории
- `orders` - количество заказов
- `ordersAmount` - общая сумма заказов
- `drr` - ДРР (Доля Рекламных Расходов)
- `ctr` - CTR (Click Through Rate)
- `cpc` - CPC (Стоимость за клик)

## Структура данных

### 🔐 tokens.json - База токенов доступа
```json
[
  {
    "token": "tok_ivanov_2024_abc123",
    "personId": "ivanov",
    "personName": "ИП Иванов",
    "status": "Токен до 21.08.2025",
    "balance": "15,420 ₽",
    "autoRenewal": true,
    "permissions": {
      "analytics": true,
      "reviews": true,
      "promotion": true,
      "content": true,
      "statistics": true,
      "editing": true,
      "reports": false
    }
  }
]
```

**Поля токена:**
- `token` - уникальный токен доступа
- `personId` - ID продавца для связи с данными
- `personName` - отображаемое имя
- `status` - статус аккаунта
- `balance` - баланс аккаунта
- `autoRenewal` - автоматическое продление
- `permissions` - права доступа к функциям

### 👥 people.json - Подключенные продавцы
```json
[]
```

**Изначально пустой массив!** Продавцы добавляются через токены.

После добавления через токен:
```
[
  {
    "id": "ivanov",
    "name": "ИП Иванов", 
    "status": "Токен до 21.08.2025",
    "balance": "15,420 ₽",
    "autoRenewal": true
  }
]
```

### 📊 cardsByPerson.json - Метрики по продавцам
```json
{
  "ivanov": [
    {
      "id": 1,
      "title": "Заказы",
      "money": 127.3,
      "earnings": 18.7,
      "isPercentage": false
    }
  ],
  "company1": [
    {
      "id": 15,
      "title": "Заказы",
      "money": 342.8,
      "earnings": 25.4,
      "isPercentage": false
    }
  ]
}
```

**Поля карточки:**
- `id` - уникальный идентификатор
- `title` - название метрики
- `money` - основное значение
- `earnings` - изменение (положительное/отрицательное)
- `isPercentage` - флаг процентного значения

## Права доступа

### Типы прав:
- `analytics` - Аналитика
- `reviews` - Отзывы и вопросы
- `promotion` - Продвижение
- `content` - Контент
- `statistics` - Статистика
- `editing` - Доступ на редактирование
- `reports` - Отчеты (**всегда false**)

### Состояния прав:
- **🔄 Серые** - до ввода токена
- **✅ Зеленые** - разрешено
- **❌ Красные** - запрещено
- **📋 Отчеты** - всегда красные для всех

## Добавление новых данных

### Добавление нового токена

1. **Откройте** `src/data/tokens.json`
2. **Добавьте** новый объект:
```json
{
  "token": "tok_newuser_2024_xyz789",
  "personId": "newuser",
  "personName": "ИП Новый пользователь",
  "status": "Токен до XX.XX.XXXX",
  "balance": "X,XXX ₽",
  "autoRenewal": true/false,
  "permissions": {
    "analytics": true/false,
    "reviews": true/false,
    "promotion": true/false,
    "content": true/false,
    "statistics": true/false,
    "editing": true/false,
    "reports": false
  }
}
```

### Добавление метрик для продавца

1. **Откройте** `src/data/cardsByPerson.json`
2. **Добавьте** ключ с `personId` и массив карточек:
```json
{
  "newuser": [
    {
      "id": 21,
      "title": "Название метрики",
      "money": 123.45,
      "earnings": 12.3,
      "isPercentage": false
    }
  ]
}
```

### Изменение стилей
Используйте CSS переменные в `src/styles/variables.css` для единообразия дизайна.

## Особенности безопасности

- ✅ **Токен обязателен** - без него доступ невозможен
- ✅ **Динамические права** - зависят от токена
- ✅ **Изоляция данных** - каждый токен видит только свои данные
- ❌ **Отчеты заблокированы** - всегда недоступны для всех пользователей